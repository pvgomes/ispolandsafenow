// Historical URL (already submitted/linked in a few places). Same
// document as /sitemap.xml, which is the canonical one to submit to
// Google Search Console.
export { GET, prerender } from "./sitemap.xml";
